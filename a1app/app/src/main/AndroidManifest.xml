<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jdiaz88.a1app">

    <!--     This permission gets register to the device! -->
    <!--    Dangerous level permissions = the user has to grant -->
    <!--    You must handle if user says yes or no to grant ... -->
    <permission android:name="edu.uic.cs478.s19.kaboom"
        android:protectionLevel="dangerous"/>
    <uses-permission android:name="edu.uic.cs478.s19.kaboom"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.A1App">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>